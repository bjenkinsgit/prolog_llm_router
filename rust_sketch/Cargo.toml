[package]
name = "prolog-router"
version = "0.1.0"
edition = "2021"

[dependencies]
# Prolog bindings - commented out for sketch (requires SWI-Prolog installed)
# swipl = "0.3"  # Provides #[derive(Unifiable)] for term conversion

# Serde for JSON serialization (LLM structured output)
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
anyhow = "1.0"
